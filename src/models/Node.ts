export class NodeData {
    label: JSX.Element | string;
}

export class Node {
    id: string;
    type: string;
    data: NodeData;

    constructor(id: number, type: string, data: ) {
        
    }
}