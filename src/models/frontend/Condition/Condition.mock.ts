import { Condition } from 'models/frontend/Condition/Condition';

export const cond25C = new Condition({
  name: '25C',
  maleMating: 3,
  lethal: false,
  femaleSterile: false,
  arrested: false,
  maturationDays: 3,
});
