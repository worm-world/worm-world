import { TopNav } from 'components/TopNav/TopNav';
import React from 'react';

const Home = (): JSX.Element => {
  return (
    <div>
      <TopNav title={'Home'} />
    </div>
  );
};

export default Home;
